# Struktur-flask-simple
🧠 Deskripsi Project

Proyek ini merupakan aplikasi berbasis Flask (Python Framework) yang terhubung dengan database PostgreSQL menggunakan SQLAlchemy sebagai ORM (Object Relational Mapper).

Aplikasi ini menampilkan data dari tabel Tb_mahasiswa yang berisi informasi NIM, Nama Mahasiswa, Alamat, dan Tanggal Lahir.

Selain itu, tersedia beberapa route sederhana seperti:

/mahasiswa → menampilkan semua data mahasiswa

/get, /post, /delete → contoh route dasar Flask

/detail/<nama> → menampilkan halaman detail berdasarkan parameter

⚙️ Aplikasi / Tools yang Dibutuhkan

🐍 Python 3.10+ – Bahasa utama untuk menjalankan Flask

🔥 Flask – Framework web utama

🧩 Flask-SQLAlchemy – Library ORM untuk koneksi ke database

🐘 PostgreSQL – Database server yang digunakan

🧠 pgAdmin 4 – (opsional) untuk memantau dan mengelola database PostgreSQL

💻 Visual Studio Code (VS Code) – Editor untuk menulis dan menjalankan kode

📦 pip (Python package manager) – Untuk menginstal dependensi

